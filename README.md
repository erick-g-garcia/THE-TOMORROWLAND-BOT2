# Proyecto-de-robert-

## Countdown configuration

To use `!countdown`, set `TML_WEEK_1_DATE` and `TML_WEEK_2_DATE` to the confirmed start times in ISO 8601 UTC format (`YYYY-MM-DDTHH:mm:ssZ`). The bot reports that the countdown is unconfigured if either value is missing or invalid.
